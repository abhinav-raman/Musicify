import { initializeApp } from "firebase/app";

const firebaseConfig = {
  // your firebase app configuration here
  // rename this file to "FirebaseApp.jsx"
};

export const app = initializeApp(firebaseConfig);